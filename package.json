{
  "name": "poe-api",
  "version": "1.0.0",
  "description": "API for POE schedule",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "build": "npm install && npx puppeteer browsers install chrome"
  },
  "dependencies": {
    "express": "^4.18.2",
    "puppeteer": "^22.0.0",
    "puppeteer-extra": "^3.3.6",
    "puppeteer-extra-plugin-stealth": "^2.11.2"
  }
}
